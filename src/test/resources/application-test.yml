server:
  shutdown: immediate

management.endpoint:
  health.cache.time-to-live: 0
  info.cache.time-to-live: 0

hmpps:
  auth:
    endpoint:
      url: http://localhost:9092/auth
oauth:
  endpoint:
    url: http://localhost:9090/auth
  client:
    id: clientid
    secret: clientsecret
  tenant:
    id: tenant-id

api.base.url:
  microsoft-graph: http://localhost:9091

email:
  domain: '@staff.com'
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: posgres
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/postgres
    username: postgres
    password: postgres
    properties:
      sslMode: DISABLE
      sslRootCert: